<%- include("partials/header"); -%>

<h1>Home</h1>
<p><%= homeContent %></p>

<% posts.forEach( function(post){ %>
        <!-- for trimming a string to 100 lettes only -->
      <%  let contentString = post.content; %>
      <%  const maxLength = 100; %>
      <%  let trimmedWord = contentString.substr(0, maxLength); %>
   <h1> <%= post.title %> </h1>
   <p> <%= trimmedWord + "...." %> 
        <a href="/posts/<%= post.title %>">Read More</a>
</p>
<% }); %>

<!-- <% for(let i=0; i< posts.length;i++){ %>
        <h1><%= posts[i].title %></h1>
        <p><%= posts[i].content %></p>
<% } %> -->


<%- include("partials/footer"); -%>
